<% provide(:title, "#{User.find(params[:id]).name}の投稿") %>

<div class="col-md-8">
  <% if @microposts.any? %>
    <h3>投稿数：<%= @microposts.count %></h3>
    <ol class="microposts">
      <%= render @microposts %>
    </ol>
    <%= paginate @microposts %>
  <% else %>
    <a href="/">home</a>
    <p>このユーザーの投稿はまだありません。</p>
  <% end %>
</div>
