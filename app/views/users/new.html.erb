<% provide(:title, 'アカウント作成') %>
<% provide(:button_text, '登録') %>

<div class="w-4/5 mx-auto my-10">
  <div class="max-w-sm mx-auto">
    <h1 class="text-2xl text-center mb-5">アカウント作成</h1>
    <div>
      <%= render 'form' %>
    </div>
  </div>
</div>